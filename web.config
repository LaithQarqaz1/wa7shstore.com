<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <!-- Redirect all HTTP requests to HTTPS -->
        <rule name="HTTP to HTTPS redirect" stopProcessing="true">
          <match url="(.*)" ignoreCase="true" />
          <conditions>
            <add input="{HTTPS}" pattern="off" ignoreCase="true" />
          </conditions>
          <action type="Redirect" url="https://{HTTP_HOST}/{R:1}" redirectType="Permanent" />
        </rule>

        <!-- Canonical host: force apex domain wa7shstore.com (avoid www) -->
        <rule name="Force apex host" stopProcessing="true">
          <match url="(.*)" ignoreCase="true" />
          <conditions logicalGrouping="MatchAll">
            <add input="{HTTPS}" pattern="on" ignoreCase="true" />
            <add input="{HTTP_HOST}" pattern="^www\.wa7shstore\.com$" ignoreCase="true" />
          </conditions>
          <action type="Redirect" url="https://wa7shstore.com/{R:1}" redirectType="Permanent" />
        </rule>

        <rule name="Admin site icon image" stopProcessing="true">
          <match url="^site-icon\.png$" ignoreCase="true" />
          <action type="Redirect" url="https://api.wa7shstore.com/site-icon.png?v=admin-20260519-01" redirectType="Found" />
        </rule>
        <rule name="Admin site icon svg legacy" stopProcessing="true">
          <match url="^site-icon\.svg$" ignoreCase="true" />
          <action type="Redirect" url="https://api.wa7shstore.com/site-icon.png?v=admin-20260519-01" redirectType="Found" />
        </rule>
        <rule name="Admin favicon legacy" stopProcessing="true">
          <match url="^favicon\.ico$" ignoreCase="true" />
          <action type="Redirect" url="https://api.wa7shstore.com/site-icon.png?v=admin-20260519-01" redirectType="Found" />
        </rule>

        <!-- Legacy deposit page path -> inline route in index -->
        <rule name="Legacy deposit redirect" stopProcessing="true">
          <match url="^edaa\.html$" ignoreCase="true" />
          <action type="Redirect" url="/index.html#/deposit" redirectType="Permanent" />
        </rule>

        <!-- Firebase Auth helper paths must be served before the SPA fallback. -->
        <rule name="Firebase auth handler helper" stopProcessing="true">
          <match url="^__/auth/handler/?$" ignoreCase="true" />
          <action type="Rewrite" url="/__/auth/handler/index.html" />
        </rule>
        <rule name="Firebase auth iframe helper" stopProcessing="true">
          <match url="^__/auth/iframe/?$" ignoreCase="true" />
          <action type="Rewrite" url="/__/auth/iframe/index.html" />
        </rule>
        <rule name="Firebase auth links helper" stopProcessing="true">
          <match url="^__/auth/links/?$" ignoreCase="true" />
          <action type="Rewrite" url="/__/auth/links.html" />
        </rule>

        <!-- SPA fallback: let index.html handle client-side routes like /login and /404 -->
        <rule name="SPA rewrite" stopProcessing="true">
          <match url=".*" ignoreCase="true" />
          <conditions logicalGrouping="MatchAll">
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
          </conditions>
          <action type="Rewrite" url="/index.html" />
        </rule>
      </rules>
    </rewrite>
    <staticContent>
      <remove fileExtension="." />
      <mimeMap fileExtension="." mimeType="text/html" />
      <remove fileExtension=".json" />
      <mimeMap fileExtension=".json" mimeType="application/json" />
    </staticContent>
    <httpProtocol>
      <customHeaders>
        <!-- HSTS: force HTTPS in browsers for one year -->
        <add name="Strict-Transport-Security" value="max-age=31536000; includeSubDomains; preload" />
        <!-- Upgrade any HTTP resources to HTTPS on HTTPS pages -->
        <add name="Content-Security-Policy" value="upgrade-insecure-requests" />
      </customHeaders>
    </httpProtocol>
  </system.webServer>
  <location path="index.html">
    <system.webServer>
      <staticContent>
        <clientCache cacheControlMode="DisableCache" />
      </staticContent>
    </system.webServer>
  </location>
  <location path="sw.js">
    <system.webServer>
      <staticContent>
        <clientCache cacheControlMode="DisableCache" />
      </staticContent>
    </system.webServer>
  </location>
  <location path="header.css">
    <system.webServer>
      <staticContent>
        <clientCache cacheControlMode="UseMaxAge" cacheControlMaxAge="30.00:00:00" />
      </staticContent>
    </system.webServer>
  </location>
  <location path="site-bundle.js">
    <system.webServer>
      <staticContent>
        <clientCache cacheControlMode="UseMaxAge" cacheControlMaxAge="30.00:00:00" />
      </staticContent>
    </system.webServer>
  </location>
  <location path="site_settings.js">
    <system.webServer>
      <staticContent>
        <clientCache cacheControlMode="DisableCache" />
      </staticContent>
    </system.webServer>
  </location>
  <location path="header.js">
    <system.webServer>
      <staticContent>
        <clientCache cacheControlMode="UseMaxAge" cacheControlMaxAge="30.00:00:00" />
      </staticContent>
    </system.webServer>
  </location>
</configuration>
